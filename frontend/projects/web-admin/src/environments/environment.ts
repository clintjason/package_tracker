export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000/api/v1',  // Adjust the URL to match your backend API URL
  wsUrl: 'ws://localhost:5000'  // Adjust the URL to match your WebSocket URL
};